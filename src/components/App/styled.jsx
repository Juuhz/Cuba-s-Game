import styled, { css } from 'styled-components';

export const Wrapper = styled.section`
	background: #29f8b5;
	height: 100vh;
	color: #FFF;
	position: relative;
	max-width: 400px;
	display: block;
	margin: 0 auto;
`;
